{% comment %}
  On-Trend Picks Carousel Section
  Safari-compatible and mobile responsive
{% endcomment %}

<style>
  .on-trend-picks {
    padding: 50px 0;
    background: {{ section.settings.background_color }};
    border-radius: 32px;
    overflow: hidden;
    margin: 0 clamp(12px, 4vw, 48px);
  }

  .on-trend-picks__header {
    text-align: center;
    margin-bottom: 40px;
  }

  .on-trend-picks__title {
    margin: 0 0 20px;
    line-height: 1.2;
    color: {{ section.settings.title_color | default: '#2b2b2b' }};
  }

  .on-trend-picks__subtitle {
    color: #666;
    margin: 0 0 20px;
    line-height: 1.5;
  }

  .on-trend-picks__carousel-wrapper {
    position: sticky;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
    border-radius: 28px;
    overflow: hidden;
  }

  .on-trend-picks__carousel {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
    gap: 0;
    padding: 40px 60px;
    align-items: center;
  }

  .on-trend-picks__carousel::-webkit-scrollbar {
    display: none;
  }

  .on-trend-picks__slide {
    flex: 0 0 33.333%;
    scroll-snap-align: center;
    position: relative;
    border-radius: 28px;
    overflow: hidden;
    background: #f5f5f5;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), 
                scale 0.4s cubic-bezier(0.4, 0, 0.2, 1),
                opacity 0.4s ease,
                box-shadow 0.4s ease,
                z-index 0.4s ease,
                margin 0.4s ease;
    cursor: pointer;
    margin: 0 -20px;
    opacity: 0.6;
    transform: scale(0.85) translateZ(0);
    z-index: 1;
  }

  .on-trend-picks__slide--active {
    transform: scale(1.1) translateZ(50px);
    opacity: 1;
    
    z-index: 10;
    margin: 0 10px;
  }

  .on-trend-picks__slide {
    cursor: pointer;
  }
  
  .on-trend-picks__slide:not(.on-trend-picks__slide--active):hover {
    opacity: 0.75;
  }

  .on-trend-picks__slide--active:hover {
    transform: scale(1.1) translateZ(50px);
  }

  .on-trend-picks__slide-image {
    width: 100%;
    aspect-ratio: 4/5;
    object-fit: cover;
    display: block;
  }

  .on-trend-picks__slide-content {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 30px 20px;
    background: linear-gradient(to top, rgba(0,0,0,0.8) 0%, transparent 100%);
    color: white;
  }

  .on-trend-picks__slide-title {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 8px;
    text-transform: uppercase;
  }

  .on-trend-picks__slide-description {
    font-size: 14px;
    margin-bottom: 15px;
    opacity: 0.9;
  }

  .on-trend-picks__slide-button {
    display: inline-block;
    padding: 10px 25px;
    background: white;
    color: #333;
    text-decoration: none;

    font-size: 14px;
    font-weight: 600;
    transition: all 0.3s ease;
    text-transform: uppercase;
  }

  .on-trend-picks__slide-button:hover {
    background: #333;
    color: white;
    transform: scale(1.05);
  }

  .on-trend-picks__progress-container {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    margin-top: 30px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    padding: 0 20px;
  }

  .on-trend-picks__progress {
    flex: 1;
    margin: 0;
  }

  .on-trend-picks__progress-line {
    position: relative;
    height: 4px;
    background: #e0e0e0;
    border-radius: 2px;
  }

  .on-trend-picks__progress-thumb {
    position: absolute;
    top: 50%;
    left: 0;
    transform: translateY(-50%);
    height: 8px;
    width: 40px;
    background: #333;
    border-radius: 4px;
    transition: left 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .on-trend-picks__arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid #e0e0e0;
    background: white;
    color: #333;
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 0;
  }

  .on-trend-picks__arrow:hover {
    background: #333;
    color: white;
    border-color: #333;
  }

  .on-trend-picks__arrow:disabled {
    opacity: 0.4;
    cursor: not-allowed;
    background: #f5f5f5;
    color: #999;
    border-color: #e0e0e0;
  }

  /* Tablet styles */
  @media (max-width: 1024px) {
    .on-trend-picks__slide {
      flex: 0 0 45%;
      margin: 0 -10px;
    }
    
    .on-trend-picks__slide--active {
      transform: scale(1.05) translateZ(30px);
      margin: 0 5px;
    }
    
    .on-trend-picks__title {
      font-size: 2.5rem;
    }
  }

  /* Mobile styles */
  @media (max-width: 768px) {
    .on-trend-picks {
      padding: 30px 0;
      border-radius: 24px;
    }

    .on-trend-picks__title {
      font-size: 2rem;
    }

    .on-trend-picks__subtitle {
      font-size: 1.25rem;
    }

    .on-trend-picks__slide {
      flex: 0 0 85%;
      margin: 0 10px;
      scroll-snap-align: center;
    }

    .on-trend-picks__carousel {
      gap: 0;
      padding: 20px;
      scroll-snap-type: x mandatory;
    }
    
    .on-trend-picks__slide--active {
      transform: scale(1);
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      margin: 0 10px;
    }
    
    .on-trend-picks__slide:not(.on-trend-picks__slide--active) {
      transform: scale(0.9);
      opacity: 0.8;
    }

    .on-trend-picks__slide-title {
      font-size: 20px;
    }

    .on-trend-picks__slide-content {
      padding: 20px 15px;
    }

    .on-trend-picks__progress-container {
      max-width: 400px;
      gap: 15px;
    }
    
    .on-trend-picks__arrow {
      width: 36px;
      height: 36px;
    }
  }

  /* Small mobile styles */
  @media (max-width: 480px) {
    .on-trend-picks__slide {
      flex: 0 0 90%;
      margin: 0 5px;
      border-radius: 20px;
    }
    
    .on-trend-picks__slide--active {
      margin: 0 5px;
    }
    
    .on-trend-picks__carousel {
      padding: 15px;
    }
    
    .on-trend-picks__progress-container {
      max-width: 300px;
      margin-top: 20px;
    }
    
    .on-trend-picks__slide-title {
      font-size: 18px;
    }
    
    .on-trend-picks__slide-description {
      font-size: 13px;
    }
    
    .on-trend-picks__slide-button {
      padding: 8px 20px;
      font-size: 13px;
    }
  }
</style>

<section class="on-trend-picks" id="on-trend-picks-{{ section.id }}">
  <div class="on-trend-picks__header">
    <h2 class="on-trend-picks__title">{{ section.settings.title }}</h2>
    <p class="on-trend-picks__subtitle">{{ section.settings.subtitle }}</p>
  </div>

  <div class="on-trend-picks__carousel-wrapper">
    <div class="on-trend-picks__carousel">
      {% for block in section.blocks %}
        <div class="on-trend-picks__slide" {{ block.shopify_attributes }}>
          {% if block.settings.image %}
            <img 
              class="on-trend-picks__slide-image"
              src="{{ block.settings.image | image_url: width: 600 }}"
              srcset="
                {{ block.settings.image | image_url: width: 400 }} 400w,
                {{ block.settings.image | image_url: width: 600 }} 600w,
                {{ block.settings.image | image_url: width: 800 }} 800w
              "
              sizes="(max-width: 768px) 85vw, (max-width: 1024px) 50vw, 33vw"
              alt="{{ block.settings.title }}"
              loading="lazy"
            >
          {% else %}
            <div class="on-trend-picks__slide-image" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);"></div>
          {% endif %}
          
          <div class="on-trend-picks__slide-content">
            <h3 class="on-trend-picks__slide-title">{{ block.settings.title }}</h3>
            <p class="on-trend-picks__slide-description">{{ block.settings.description }}</p>
            {% if block.settings.button_text != blank and block.settings.button_link != blank %}
              <a href="{{ block.settings.button_link }}" class="on-trend-picks__slide-button">
                {{ block.settings.button_text }}
              </a>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>

  <div class="on-trend-picks__progress-container">
    <button type="button" class="on-trend-picks__arrow on-trend-picks__arrow--prev" aria-label="Previous slide">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M15 19L8 12L15 5" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
    
    <div class="on-trend-picks__progress">
      <div class="on-trend-picks__progress-line">
        <div class="on-trend-picks__progress-thumb"></div>
      </div>
    </div>

    <button type="button" class="on-trend-picks__arrow on-trend-picks__arrow--next" aria-label="Next slide">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M9 5L16 12L9 19" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
  </div>
</section>

<script>
  (function() {
    const sectionId = 'on-trend-picks-{{ section.id }}';
    const section = document.getElementById(sectionId);
    if (!section) return;

    const carousel = section.querySelector('.on-trend-picks__carousel');
    const slides = carousel.querySelectorAll('.on-trend-picks__slide');
    const progressThumb = section.querySelector('.on-trend-picks__progress-thumb');
    const prevArrow = section.querySelector('.on-trend-picks__arrow--prev');
    const nextArrow = section.querySelector('.on-trend-picks__arrow--next');
    
    if (slides.length === 0) return;

    let currentIndex = 1; // Start with second slide (will be centered)
    let isProgrammaticScroll = false;
    let scrollTimeout;
    
    // Get number of visible slides based on screen width
    function getVisibleSlides() {
      if (window.innerWidth <= 480) return 1;
      if (window.innerWidth <= 768) return 1;
      if (window.innerWidth <= 1024) return 2;
      return 3;
    }
    
    // Update active slide styling
    function updateActiveSlide() {
      slides.forEach((slide, index) => {
        if (index === currentIndex) {
          slide.classList.add('on-trend-picks__slide--active');
        } else {
          slide.classList.remove('on-trend-picks__slide--active');
        }
      });
    }
    
    // Update progress bar & arrows
    function updateProgress() {
      if (!progressThumb) return;
      const maxIndex = slides.length - 1;
      const percentage = maxIndex > 0 ? (currentIndex / maxIndex) : 0;
      
      // Math equation for centering / sliding the thumb perfectly
      progressThumb.style.left = `calc(${percentage * 100}% - ${percentage * 40}px)`;
      
      if (prevArrow && nextArrow) {
        prevArrow.disabled = (currentIndex === 0);
        nextArrow.disabled = (currentIndex === maxIndex);
      }
    }
    
    // Scroll to slide
    function scrollToSlide(index) {
      index = Math.max(0, Math.min(index, slides.length - 1));
      
      isProgrammaticScroll = true;
      currentIndex = index;
      
      const targetSlide = slides[index];
      const slideCenter = targetSlide.offsetLeft + (targetSlide.offsetWidth / 2);
      const carouselCenter = carousel.offsetWidth / 2;
      
      // Calculate scroll position to center the slide
      let scrollPosition = slideCenter - carouselCenter;
      
      // Ensure we don't scroll past boundaries
      const maxScroll = carousel.scrollWidth - carousel.offsetWidth;
      scrollPosition = Math.max(0, Math.min(scrollPosition, maxScroll));
      
      carousel.scrollTo({
        left: scrollPosition,
        behavior: 'smooth'
      });
      
      updateProgress();
      updateActiveSlide();
      
      // Reset the programmatic scroll lockout flag after scrolling completes
      clearTimeout(scrollTimeout);
      scrollTimeout = setTimeout(() => {
        isProgrammaticScroll = false;
      }, 500);
    }
    
    // Handle carousel scroll natively
    function handleScroll() {
      if (isProgrammaticScroll) return;
      
      const scrollLeft = carousel.scrollLeft;
      const carouselCenter = scrollLeft + (carousel.offsetWidth / 2);
      
      // Find which slide is closest to center
      let centerIndex = 0;
      let minDistance = Infinity;
      
      slides.forEach((slide, index) => {
        const slideCenter = slide.offsetLeft + (slide.offsetWidth / 2);
        const distance = Math.abs(slideCenter - carouselCenter);
        
        if (distance < minDistance) {
          minDistance = distance;
          centerIndex = index;
        }
      });
      
      if (centerIndex !== currentIndex) {
        currentIndex = centerIndex;
        updateProgress();
        updateActiveSlide();
      }
    }
    
    // Scroll event listener using requestAnimationFrame for optimal performance
    let ticking = false;
    carousel.addEventListener('scroll', () => {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          handleScroll();
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });
    
    // Arrow button controls
    if (prevArrow) {
      prevArrow.addEventListener('click', () => {
        scrollToSlide(currentIndex - 1);
      });
    }

    if (nextArrow) {
      nextArrow.addEventListener('click', () => {
        scrollToSlide(currentIndex + 1);
      });
    }
    
    // Click on slides to select them
    slides.forEach((slide, index) => {
      slide.addEventListener('click', (e) => {
        // Don't advance if clicking a button
        if (e.target.closest('.on-trend-picks__slide-button')) {
          return;
        }
        
        // If clicking on an inactive slide, make it active
        if (index !== currentIndex) {
          scrollToSlide(index);
        }
      });
    });
    
    // Handle resize
    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        scrollToSlide(Math.min(currentIndex, slides.length - getVisibleSlides()));
      }, 250);
    });
    
    // Initialize
    updateProgress();
    
    // Start with slide 2 centered on page load
    if (slides.length > 1) {
      setTimeout(() => {
        scrollToSlide(1); // Start with second slide (index 1)
      }, 100);
    } else {
      updateActiveSlide();
    }
  })();
</script>

{% schema %}
{
  "name": "On-Trend Picks",
  "tag": "section",
  "class": "section-on-trend-picks",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "ON-TREND PICKS"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Explore Our Promising Line-up"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color",
      "default": "#2b2b2b"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Collection Title"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "Discover our latest collection"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button Text",
          "default": "Explore Now"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "On-Trend Picks",
      "blocks": [
        {
          "type": "slide",
          "settings": {
            "title": "BIG TEE ENERGY",
            "description": "Oversized Fit Done Right",
            "button_text": "EXPLORE NOW"
          }
        },
        {
          "type": "slide",
          "settings": {
            "title": "SLEEP DRESS EDIT",
            "description": "For dreams and downtime",
            "button_text": "EXPLORE NOW"
          }
        },
        {
          "type": "slide",
          "settings": {
            "title": "SUMMER SHORTS",
            "description": "Made for every plan",
            "button_text": "EXPLORE NOW"
          }
        }
      ]
    }
  ]
}
{% endschema %}
